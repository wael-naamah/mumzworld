import tokens from './tokens';

const colors = {
  background: tokens.color.semanticBgWhite,
  // TODO: Add more colors
};

// TODO: Add dark mode colors
export default {
  dark: colors,
  light: colors,
};
